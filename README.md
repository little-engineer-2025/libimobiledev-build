# Build libimobiledevice

Makefile to build libimobiledevice repositories quickly.

```sh
make deps
make build
```

To clean the environment run:

```sh
make clean
```

This is installed at ~/libimobiledevice

